package lexical_analysis.tokens.logical_operator;

public class XorToken extends LogicalOperatorToken {
    public XorToken(int row, int column, String content) {
        super(row, column, content);
    }
}
