package lexical_analysis.tokens.predicate;

public class IsRealToken extends PredicateToken {
    public IsRealToken(int row, int column, String content) {
        super(row, column, content);
    }
}
