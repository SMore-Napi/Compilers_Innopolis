package lexical_analysis.tokens.predicate;

public class IsAtomToken extends PredicateToken {
    public IsAtomToken(int row, int column, String content) {
        super(row, column, content);
    }
}
