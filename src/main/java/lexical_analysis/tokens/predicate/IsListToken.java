package lexical_analysis.tokens.predicate;

public class IsListToken extends PredicateToken {
    public IsListToken(int row, int column, String content) {
        super(row, column, content);
    }
}
