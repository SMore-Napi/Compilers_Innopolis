package lexical.logical_operators;

import lexical.Token;

public class NotToken extends Token {
    public NotToken(int row, int column, String content) {
        super(row, column, content);
    }
}
