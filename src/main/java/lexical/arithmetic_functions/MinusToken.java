package lexical.arithmetic_functions;

import lexical.Token;

public class MinusToken extends Token {
    public MinusToken(int row, int column, String content) {
        super(row, column, content);
    }
}
