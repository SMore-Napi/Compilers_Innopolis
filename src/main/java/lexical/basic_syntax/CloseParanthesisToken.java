package lexical.basic_syntax;

import lexical.Token;

public class CloseParanthesisToken extends Token {

    public CloseParanthesisToken(int row, int column, String content) {
        super(row, column, content);
    }
}
