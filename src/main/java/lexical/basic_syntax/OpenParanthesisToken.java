package lexical.basic_syntax;

import lexical.Token;

public class OpenParanthesisToken extends Token {

    public OpenParanthesisToken(int row, int column, String content) {
        super(row, column, content);
    }
}
