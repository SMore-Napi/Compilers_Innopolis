package lexical.basic_syntax;

import lexical.Token;

public class QuoteShortToken extends Token {
    public QuoteShortToken(int row, int column, String content) {
        super(row, column, content);
    }
}
