package lexical.tokens.comparison;

import lexical.tokens.Token;

public class ComparisonToken extends Token {
    public ComparisonToken(int row, int column, String content) {
        super(row, column, content);
    }
}
