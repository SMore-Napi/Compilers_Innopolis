package lexical.tokens.predicate;

import lexical.tokens.Token;

public class PredicateToken extends Token {
    public PredicateToken(int row, int column, String content) {
        super(row, column, content);
    }
}
