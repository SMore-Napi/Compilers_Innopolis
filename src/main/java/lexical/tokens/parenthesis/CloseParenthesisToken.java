package lexical.tokens.parenthesis;

public class CloseParenthesisToken extends ParenthesisToken {

    public CloseParenthesisToken(int row, int column, String content) {
        super(row, column, content);
    }
}
