package lexical.tokens.parenthesis;

import lexical.tokens.Token;

public class ParenthesisToken extends Token {
    public ParenthesisToken(int row, int column, String content) {
        super(row, column, content);
    }
}
