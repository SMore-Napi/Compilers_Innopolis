package lexical.tokens.parenthesis;

public class OpenParenthesisToken extends ParenthesisToken {

    public OpenParenthesisToken(int row, int column, String content) {
        super(row, column, content);
    }
}
