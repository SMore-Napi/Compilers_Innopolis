package lexical.tokens.operation_on_lists;

public class ConsToken extends OperationOnListsToken {
    public ConsToken(int row, int column, String content) {
        super(row, column, content);
    }
}
