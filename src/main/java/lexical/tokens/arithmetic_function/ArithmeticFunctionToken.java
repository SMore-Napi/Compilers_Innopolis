package lexical.tokens.arithmetic_function;

import lexical.tokens.Token;

public class ArithmeticFunctionToken extends Token {
    public ArithmeticFunctionToken(int row, int column, String content) {
        super(row, column, content);
    }
}
