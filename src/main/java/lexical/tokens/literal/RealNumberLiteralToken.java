package lexical.tokens.literal;

public class RealNumberLiteralToken extends LiteralToken {
    public RealNumberLiteralToken(int row, int column, String content) {
        super(row, column, content);
    }
}
