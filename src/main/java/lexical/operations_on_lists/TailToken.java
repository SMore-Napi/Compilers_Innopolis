package lexical.operations_on_lists;

import lexical.Token;

public class TailToken extends Token {
    public TailToken(int row, int column, String content) {
        super(row, column, content);
    }
}
