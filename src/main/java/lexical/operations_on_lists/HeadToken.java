package lexical.operations_on_lists;

import lexical.Token;

public class HeadToken extends Token {
    public HeadToken(int row, int column, String content) {
        super(row, column, content);
    }
}
