package lexical.comparisions;

import lexical.Token;

public class LessEqToken extends Token {
    public LessEqToken(int row, int column, String content) {
        super(row, column, content);
    }
}
