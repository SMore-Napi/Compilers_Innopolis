package lexical.comparisions;

import lexical.Token;

public class EqualToken extends Token {
    public EqualToken(int row, int column, String content) {
        super(row, column, content);
    }
}
