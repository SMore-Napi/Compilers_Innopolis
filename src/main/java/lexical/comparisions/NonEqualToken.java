package lexical.comparisions;

import lexical.Token;

public class NonEqualToken extends Token {
    public NonEqualToken(int row, int column, String content) {
        super(row, column, content);
    }
}
