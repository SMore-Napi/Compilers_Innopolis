package lexical.comparisions;

import lexical.Token;

public class GreaterToken extends Token {
    public GreaterToken(int row, int column, String content) {
        super(row, column, content);
    }
}
