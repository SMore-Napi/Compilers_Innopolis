package lexical.predicates;

import lexical.Token;

public class IsNullToken extends Token {
    public IsNullToken(int row, int column, String content) {
        super(row, column, content);
    }
}
