package lexical.predicates;

import lexical.Token;

public class IsAtomToken extends Token {
    public IsAtomToken(int row, int column, String content) {
        super(row, column, content);
    }
}
