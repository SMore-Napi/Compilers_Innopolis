package lexical.predicates;

import lexical.Token;

public class IsBoolToken extends Token {
    public IsBoolToken(int row, int column, String content) {
        super(row, column, content);
    }
}
